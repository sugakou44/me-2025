import { float } from '@/lib/utils/data'

export const BLADE_COUNT = 512 * 1024
export const PATCH_SIZE = float(10)
export const BLADE_WIDTH = float(0.005)
export const BLADE_HEIGHT = float(0.1)
export const STIFFNESS = float(0.5)
